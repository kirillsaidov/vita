#include "vita/algorithm/comparison.h"

int8_t vt_cmp_mini8(const int8_t x, const int8_t y) {
    return x < y ? x : y;
}

uint8_t vt_cmp_minu8(const uint8_t x, const uint8_t y) {
    return x < y ? x : y;
}

int16_t vt_cmp_mini16(const int16_t x, const int16_t y) {
    return x < y ? x : y;
}

uint16_t vt_cmp_minu16(const uint16_t x, const uint16_t y) {
    return x < y ? x : y;
}

int32_t vt_cmp_mini32(const int32_t x, const int32_t y) {
    return x < y ? x : y;
}

uint32_t vt_cmp_minu32(const uint32_t x, const uint32_t y) {
    return x < y ? x : y;
}

int64_t vt_cmp_mini64(const int64_t x, const int64_t y) {
    return x < y ? x : y;
}

uint64_t vt_cmp_minu64(const uint64_t x, const uint64_t y) {
    return x < y ? x : y;
}

float vt_cmp_minf(const float x, const float y) {
    return x < y ? x : y;
}

double vt_cmp_mind(const double x, const double y) {
    return x < y ? x : y;
}

real vt_cmp_minr(const real x, const real y) {
    return x < y ? x : y;
}

int8_t vt_cmp_maxi8(const int8_t x, const int8_t y) {
    return x > y ? x : y;
}

uint8_t vt_cmp_maxu8(const uint8_t x, const uint8_t y) {
    return x > y ? x : y;
}

int16_t vt_cmp_maxi16(const int16_t x, const int16_t y) {
    return x > y ? x : y;
}

uint16_t vt_cmp_maxu16(const uint16_t x, const uint16_t y) {
    return x > y ? x : y;
}

int32_t vt_cmp_maxi32(const int32_t x, const int32_t y) {
    return x > y ? x : y;
}

uint32_t vt_cmp_maxu32(const uint32_t x, const uint32_t y) {
    return x > y ? x : y;
}

int64_t vt_cmp_maxi64(const int64_t x, const int64_t y) {
    return x > y ? x : y;
}

uint64_t vt_cmp_maxu64(const uint64_t x, const uint64_t y) {
    return x > y ? x : y;
}

float vt_cmp_maxf(const float x, const float y) {
    return x > y ? x : y;
}

double vt_cmp_maxd(const double x, const double y) {
    return x > y ? x : y;
}

real vt_cmp_maxr(const real x, const real y) {
    return x > y ? x : y;
}


